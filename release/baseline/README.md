# Release baseline

`R15-FILE-MANIFEST.json` is the immutable file-manifest baseline used by the
R17 preparation and source-integrity tooling. It is retained because current
qualification compares the working source lineage against this reviewed
baseline.

Historical release reports are intentionally not shipped in the current public
source bundle. Git tags/releases are the archival authority for older reports.
