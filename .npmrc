# Official npm endpoint reachable from restricted cloud networks. The lockfile
# remains authoritative: frozen installs verify every package integrity hash.
registry=https://registry.npmjs.com/
fetch-retries=6
fetch-retry-factor=2
fetch-retry-mintimeout=1000
fetch-retry-maxtimeout=60000
fetch-timeout=180000
network-concurrency=4
